/**
 * 마이스윗 테스트용 광고 설정.
 * 실제 판정 로직은 사이트 전역 공용 모듈(@/lib/ads-config)에 있고, 여기서는 재노출만 한다.
 */
export {
  AD_WIDTH,
  AD_HEIGHT,
  isAdPreview,
  adfitUnitId,
  coupangAffiliateUrl,
  type AdFitPlacement,
} from "@/lib/ads-config";
